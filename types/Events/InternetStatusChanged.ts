/**
 * The data included with the "onInternetStatusChanged" event.
 *
 * Invoked when internet status has changed.
 *
 * @event onInternetStatusChanged
 * @see {@link OpenpathEvent}
 */
export interface InternetStatusChanged {
  isInternetAvailable: Boolean;
}
